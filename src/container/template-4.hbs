<header class="header">
    <div>
        {{> logo}}
    </div>

    <nav class="header_nav">
        <div class="header_nav_right">
            {{> nav list=header}}
        </div>
    </nav>
</header>

{{#with main}}
<main class="main">
    <img src="{{image}}" alt="photo" class="main_image">
    <div class="main_content">
        <h1 class="main_title">{{heading}}</h1>
        <p class="main_description">{{descriotion}}</p>
        <a href="{{href}}" class="main_button">{{button}}</a>
    </div>
</main>
{{/with}}

<div class="featured">
    {{#each featured}}
    <div class="featured_content">
        <img src="{{photo}}" alt="" class="featured_image">
        <h3 class="featured_title">{{heading}}</h3>
        <p class="featured_info">{{info}}</p>
        <a href="{{linkUrl}}" class="featured_button">{{linkText}}</a>
    </div>
    {{/each}}
</div>

<div class="actions">
    {{#each actions}}
    <div class="actions_cards">
        <div class="actions_heading">{{heading}}</div>
        <a href="{{url}}" class="actions_button">{{text}}</a>
    </div>
    {{/each}}
</div>

<footer class="footer">
    <div>
        {{> logo}}
        <span class="copyright">Copyright 2023-2024</span>
    </div>

    {{#each footer}}
    <div>
        <span class="footer_title">{{title}}</span>
        {{> nav list=links isVertical=true}}
    </div>
    {{/each}}
</footer>